jwt:
  secret: oneweek
  validity: 86400
  caching: 86400

first-coordinator-email: be@oneweek.ua

spring:
  security:
    oauth2:
      client:
        registration:
          facebook:
            clientId: 123456
            clientSecret: qwerty
            redirectUri: uriuri

  data:
    mongodb:
      uri: mongodb://${MONGODB_HOSTNAME}:${MONGODB_PORT}/${MONGODB_DATABASE}
      username: ${MONGODB_USER}
      password: ${MONGODB_PASSWORD}

redis:
  host: ${REDIS_HOSTNAME}
  port: ${REDIS_PORT}